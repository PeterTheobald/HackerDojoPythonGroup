[project]
name = "fire-challenge"
version = "1.0.0"
description = "A Python module for grid-based fire spread challenges"
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
]
