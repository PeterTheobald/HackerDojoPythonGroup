[project]
name = "fire-challenge"
version = "1.0.0"
description = "A Python module for grid-based fire spread challenges"
requires-python = ">=3.8"
dependencies = [
    "matplotlib>=3.7.0",
    "networkx>=3.1",
    "numpy>=1.24.0",
]
